# 화캉스 데스크톱 위젯

바탕화면 구석에 항상 떠 있는 화장쉼 스톱워치 위젯입니다. 둥근 흰색 카드 + 우측 하단에 삐져나온 변기 일러스트 형태의 알림 카드 스타일이고, Electron으로 감싸서 테두리 없이 항상 위에 떠 있는 작은 창으로 띄웁니다.

## 실행 전 준비물

- [Node.js](https://nodejs.org) (LTS 버전 설치 — 설치하면 `npm`도 같이 설치됩니다)

## 처음 실행해보기 (설치 파일 없이 바로 테스트)

터미널(명령 프롬프트/PowerShell)을 열고 이 폴더로 이동한 뒤:

```bash
npm install
npm start
```

화면 오른쪽 아래에 위젯이 뜨고, 작업표시줄 우측 트레이 아이콘에도 화캉스 아이콘이 생깁니다. 트레이 아이콘을 클릭하면 보이기/숨기기, 우클릭하면 위치 재설정·종료 메뉴가 나옵니다.

## 진짜 설치파일(.exe) 만들기

```bash
npm run build
```

`dist/` 폴더에 `화캉스 위젯 x.x.x.exe` (portable, 설치 없이 더블클릭으로 실행되는 단일 실행파일)가 생성됩니다. 이 파일만 원하는 곳에 복사해두고 실행하면 됩니다.

## 특징

- 테두리 없는 작은 창, 항상 다른 창 위에 떠 있음(always-on-top)
- 위젯 아무 곳이나 드래그해서 원하는 위치로 옮길 수 있음(버튼·ASMR 아이콘 제외)
- **드래그로 옮긴 위치는 자동 저장**되어 앱을 껐다 켜도 마지막 위치를 기억합니다
- 모니터가 여러 대면 트레이 아이콘 우클릭 메뉴에서 원하는 모니터의 우측 하단으로 바로 이동 가능
- 작업표시줄에는 안 보이고, 시스템 트레이 아이콘으로만 존재
- 기록은 이 PC(Electron 앱의 로컬 저장소)에만 저장됩니다 — 브라우저 버전과는 별개의 저장 공간이라 서로 동기화되지 않아요

## 알려진 제약

- Windows의 "바탕화면 보기"(Win+D)를 눌렀을 때도 위젯이 계속 보이는지는 Node.js가 없는 이 작업환경에서 직접 테스트하지 못했습니다. 만약 다른 창과 함께 최소화된다면 알려주세요 — 별도 처리를 추가할 수 있어요.

## 커스터마이즈

- **아이콘**: `icon.png`를 원하는 정사각형 이미지로 교체하면 트레이 아이콘이 바뀝니다.
- **크기/위치**: `main.js` 상단의 `WIDGET_WIDTH`, `WIDGET_HEIGHT`, `MARGIN` 값을 바꾸면 됩니다.
- **화면 내용**: `widget.html`은 이 카드 디자인 전용 파일이라(브라우저 미니 위젯과는 다른 레이아웃), 직접 열어서 CSS를 수정하면 됩니다. 오늘의 기록 목록은 카드가 작아서 뺐고, 상태·경과시간·오늘 총합만 보여줘요.

## 참고

- 이 프로젝트는 소스코드만 작성된 상태입니다. 실제 `.exe`를 만들려면 Node.js가 설치된 PC에서 위 `npm run build` 과정을 직접 실행해야 해요(이 작업환경에는 Node.js가 없어서 미리 빌드해드리지 못했습니다).
- Mac에서 만들고 싶다면 `package.json`의 `build.win` 대신 `build.mac`을 추가하고 `electron-builder --mac`으로 빌드하면 됩니다.
