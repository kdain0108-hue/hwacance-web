# 화캉스 작업 에이전트 지침

이 문서는 화캉스 저장소에서 작업하는 코딩 에이전트가
제품 의도와 기술적 경계를 일관되게 지키기 위한 실행 규칙이다.

## 1. 지침 우선순위

충돌이 있을 때 다음 순서를 따른다.

1. 현재 사용자가 직접 제공한 지시
2. 이 `agent.md`
3. [PRD.md](./PRD.md)의 제품 결정
4. 변경 대상과 연결된 `spec/` 상세 사양
5. 기존 코드의 관례

상위 지침과 하위 문서가 충돌하면 조용히 한쪽을 무시하지 않는다.
요구사항 변경이 명확하면 관련 문서를 코드와 함께 갱신한다.

## 2. 제품 미션

화캉스는 직장인이 화장실에서 보내는 짧은 휴식을
시간 걱정 없이 보내고 부담 없이 일상으로 돌아가도록 돕는 모바일 웹이다.

제품의 핵심은 다음 세 가지다.

- 픽셀 변기와 원형 진행률로 남은 시간을 보여주는 타이머
- 유쾌한 창작 위로 문장 따라쓰기
- 모든 주요 화면에서 제어할 수 있는 단일 ASMR

## 3. 확정된 제품 결정

다음 결정은 사용자 요청 없이 변경하지 않는다.

- 첫 출시는 일반 모바일 웹이다.
- 기본 진입 화면은 타이머다.
- 하단 내비게이션은 왼쪽부터 `눈치껏 쉬자`, `타이머`, `부처되기` 세 탭이다.
- 눈치껏 쉬자는 마지막 화장쉼 경과 시간에 따라 3단계 안내 문구를 보여준다.
- 타이머를 그만 쉬기 또는 체크아웃으로 마치면 시작 시각이 눈치껏 쉬자에 자동 기록된다.
- ASMR ON/OFF는 모든 주요 화면의 오른쪽 상단에 둔다.
- ASMR 기본값은 OFF다.
- 메인 컬러는 흰색과 파란색 `#6485F6`이다.
- 타이머는 5분, 10분, 15분과 1~60분 직접 입력을 지원한다.
- 타이머 실행 중 원형 진행 표시가 시간에 맞춰 줄어든다.
- 타이머 시작 전 픽셀 변기는 닫혀 있고 시작 후 열린다.
- 타이머 완료 문구는 `화캉스 체크아웃 해야 해요`다.
- 문장 콘텐츠는 실제 인용이 아닌 창작 위로 문장 100개다.
- 필사 완료 화면에는 목록, 다음 필사, 이미지 저장 행동이 있다.
- 목록에는 이 브라우저에서 완료한 문장만 표시한다.
- 이미지 저장은 현재 필사 완료 앱 화면을 그대로 PNG로 캡처한다.
- 회원가입, 서버 동기화, PWA, 웹 푸시, 게임은 MVP 범위가 아니다.
- 잠금·백그라운드·전원 종료 상태의 정시 진동을 약속하지 않는다.

## 4. 상세 사양 라우팅

작업 전 변경 영역에 맞는 문서를 읽는다.

| 변경 영역 | 필수 문서 |
| --- | --- |
| 제품 목표·범위·성공 기준 | [PRD.md](./PRD.md) |
| 공통 레이아웃·내비게이션·접근성 | [spec/platform-and-ux.md](./spec/platform-and-ux.md) |
| 시간 설정·진행·완료·복원 | [spec/timer.md](./spec/timer.md) |
| 문장·입력·완료 목록·이미지 저장 | [spec/quote-writing.md](./spec/quote-writing.md) |
| ASMR 토글·재생·오류·라이선스 | [spec/asmr.md](./spec/asmr.md) |
| 화장쉼 기록·경과 시간·안내 문구 | [spec/break-tracker.md](./spec/break-tracker.md) |

두 기능에 걸친 변경은 각 기능 문서와 공통 사양을 함께 확인한다.

## 5. 구현 기본 원칙

### 5.1 단순성

- 가입, 온보딩, 불필요한 모달을 추가하지 않는다.
- MVP에 필요한 최소 상태와 최소 화면만 구현한다.
- 기능 하나를 위해 서버를 새로 도입하지 않는다.
- 추상화는 실제 중복이나 명확한 경계가 있을 때만 추가한다.

### 5.2 모바일 우선

- 320~430px 세로 화면을 먼저 설계한다.
- 핵심 콘텐츠 최대 너비는 480px다.
- 주요 터치 영역은 최소 44×44px로 만든다.
- 운영체제 안전 영역과 모바일 키보드를 고려한다.
- 데스크톱에서는 모바일 콘텐츠를 중앙에 배치한다.

### 5.3 조용한 기본값

- 페이지 진입만으로 소리를 재생하지 않는다.
- ASMR은 사용자의 명시적 ON 동작 뒤에만 재생한다.
- 시스템 음량이나 무음 설정을 우회하지 않는다.
- 소리가 없어도 완료와 오류를 화면으로 이해할 수 있어야 한다.

## 6. 타이머 구현 규칙

- 남은 시간은 반복 호출 횟수가 아니라 절대 종료 시각으로 계산한다.
- 기준 공식은 `remaining = max(0, endAt - now)`다.
- `setInterval`은 화면 갱신 수단일 뿐 시간의 원본이 아니다.
- 실행 상태에는 전체 시간, 종료 시각, 일시정지 잔여 시간이 포함된다.
- 새로고침 후 종료 시각이 남아 있으면 상태를 복원한다.
- 백그라운드에서 종료되었으면 복귀 즉시 완료 상태를 보여준다.
- 백그라운드에서 실제 알림이 울렸다고 가정하지 않는다.
- 진동 실패는 전체 완료 흐름의 실패가 아니다.
- 대기·설정 중에는 닫힌 변기, 실행 중에는 열린 변기를 표시한다.
- 원형 진행률은 실행 중 열린 변기 주변에 배치한다.
- 원형 진행률과 `MM:SS` 텍스트를 함께 제공한다.
- 그만 쉬기 또는 체크아웃으로 idle에 돌아가면 눈치껏 쉬자에 화장쉼을 자동 기록한다(연장은 같은 화장쉼으로 취급).
- 자세한 상태 전이는 [spec/timer.md](./spec/timer.md)를 따른다.

## 7. 문장 따라쓰기 구현 규칙

- 콘텐츠는 고정된 100개 목록에서 선택한다.
- 한 순환 안에서는 같은 문장을 반복하지 않는다.
- 실제 불교 구절이나 실제 인물의 말처럼 표시하지 않는다.
- 입력값은 현재 문장과의 일치 여부를 판정하는 데만 사용한다.
- 따라 쓴 텍스트를 서버나 분석 도구로 전송하지 않는다.
- 오타가 있어도 키보드를 닫거나 입력을 막지 않는다.
- 완료한 문장 ID는 기기에 저장하고 완료 목록에만 사용한다.
- 좋아요 상태는 구현하지 않는다.
- 이미지 저장은 별도 디자인을 만들지 않고 완료 화면의 앱 영역을 캡처한다.
- 이미지 생성과 저장 과정에서 서버에 화면이나 문장을 전송하지 않는다.
- 완료 조건과 표시 방식은 [spec/quote-writing.md](./spec/quote-writing.md)를 따른다.

## 8. ASMR 구현 규칙

- 앱 전체에서 하나의 오디오 인스턴스와 하나의 재생 상태를 사용한다.
- 화면별로 별도의 오디오가 동시에 재생되지 않게 한다.
- OFF 동작은 즉시 사용자에게 피드백을 주고 300ms 안에 정지한다.
- 화면 전환으로 재생이 끊기지 않게 전역 레이아웃에서 관리한다.
- 새로고침 후에는 항상 OFF로 시작한다.
- 브라우저가 재생을 거부하면 상태를 OFF로 되돌린다.
- 배포 권한이 확인된 음원만 저장소와 서비스에 포함한다.
- 자세한 동작은 [spec/asmr.md](./spec/asmr.md)를 따른다.

## 9. 상태와 개인정보

MVP는 서버 데이터베이스를 요구하지 않는다.

기기 안에 저장할 수 있는 값:

- 타이머 전체 시간
- 타이머 종료 예정 시각
- 일시정지 시 남은 시간
- 현재 문장 ID
- 이미 본 문장 ID 목록
- 완료한 문장 ID와 완료 시각 목록

저장하거나 전송하지 않는 값:

- 사용자가 따라 쓴 입력 내용
- 이름, 이메일, 회사, 위치
- 마이크 입력이나 주변 소리
- 개인별 ASMR 사용 이력

로컬 저장소가 실패해도 현재 탭의 핵심 기능은 가능한 범위에서 계속 동작해야 한다.

## 10. 접근성 규칙

- 색상만으로 선택, 오류, 진행 상태를 전달하지 않는다.
- 모든 조작 요소에 의미 있는 접근성 이름을 제공한다.
- ASMR 토글 이름에는 현재 동작인 `켜기` 또는 `끄기`를 포함한다.
- 키보드 포커스 표시를 제거하지 않는다.
- 원형 진행 표시 외에 남은 시간 텍스트를 제공한다.
- 모션 감소 설정에서는 도장과 진행 애니메이션을 최소화한다.
- 본문과 배경은 WCAG AA 수준의 명도 대비를 만족한다.

## 11. 오류 처리 규칙

- 오류 메시지는 짧고 사용자가 다음 행동을 알 수 있게 작성한다.
- 지원하지 않는 브라우저 기능은 점진적 향상으로 처리한다.
- 진동, 화면 깨우기, 오디오 중 하나가 실패해도 타이머 상태는 보존한다.
- 유효하지 않은 시간 입력은 시작 전에 차단하고 이유를 입력란 가까이에 표시한다.
- 예상하지 못한 오류가 발생해도 새로고침으로 기본 화면에 복구할 수 있어야 한다.

## 12. 테스트 기대사항

기능 변경에는 변경 범위에 맞는 테스트가 필요하다.

- 시간 계산 로직은 단위 테스트로 검증한다.
- 일시정지·재개·완료·새로고침 복원 상태 전이를 검증한다.
- 입력값 경계인 1분과 60분, 잘못된 값을 검증한다.
- 문장 일치·불일치·완료 판정을 검증한다.
- 완료한 문장만 목록에 표시되는지 검증한다.
- 필사 완료 앱 화면이 PNG로 생성되는지 검증한다.
- ASMR의 사용자 동작 전 자동 재생이 없는지 검증한다.
- 320px 화면에서 가로 스크롤과 잘린 핵심 버튼이 없는지 확인한다.
- 진동과 오디오가 지원되지 않는 환경에서도 완료 흐름을 확인한다.

테스트 도구가 없는 초기 단계라면 구현과 함께 최소 검증 체계를 마련한다.

## 13. 문서 유지 규칙

- 제품 방향이 바뀌면 `PRD.md`를 수정한다.
- 기능의 상세 동작이 바뀌면 해당 `spec/` 파일을 수정한다.
- 공통 화면이나 브라우저 정책이 바뀌면 공통 사양을 수정한다.
- 요구사항 ID를 삭제할 때 참조 링크가 남아 있지 않은지 확인한다.
- 같은 상세 요구사항을 여러 문서에 복사하지 않고 한 곳을 원본으로 둔다.
- 모든 PRD와 spec 문서는 300줄을 넘기지 않는다.

## 14. 금지 사항

명시적 요청 없이 다음을 추가하지 않는다.

- 사용자 계정과 인증
- 서버 데이터베이스
- 광고와 결제
- 위치, 마이크, 연락처 권한
- 웹 푸시 알림
- PWA 설치 유도
- 복수 음원 선택
- 실제 불교 구절을 가장한 생성 문장
- 똥 넣기 게임

## 15. 완료 정의

작업 완료를 보고하기 전에 다음을 확인한다.

- 변경이 연결된 PRD와 spec을 만족한다.
- 관련 테스트와 정적 검사가 통과한다.
- 모바일 핵심 흐름을 실제 또는 동등한 브라우저 환경에서 확인한다.
- 접근성 이름, 포커스, 터치 영역을 확인한다.
- 새로 추가한 자산의 사용 권한이 확인되어 있다.
- 개인 데이터를 새로 수집하거나 전송하지 않는다.
- 문서와 구현이 서로 다른 동작을 설명하지 않는다.
- 의도한 파일만 변경되었다.
